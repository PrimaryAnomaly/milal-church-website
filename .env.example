# Required for admin login (/admin/login). Use a long random string in production.
ADMIN_SECRET=change-me-to-a-long-random-secret

# Optional. When unset, image uploads fall back gracefully (no Blob uploads).
BLOB_READ_WRITE_TOKEN=
